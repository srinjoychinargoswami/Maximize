name: kinetic
description: "Your Energy Operating System. Track energy, understand your peak productivity window, schedule work when you're ready."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.1+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  table_calendar: ^3.0.0
  drift: ^2.16.0
  drift_flutter: ^0.2.8
  sqlite3_flutter_libs: ^0.5.8
  path: ^1.8.3
  json_annotation: ^4.9.0  # Check for the latest version
  json_serializable: ^6.8.0
  provider: ^6.0.1
  shared_preferences: ^2.2.0
  uuid: ^4.2.0
  flutter_colorpicker: ^1.1.0
  timezone: ^0.9.4
  flutter_timezone: ^4.1.1
  http: ^1.4.0
  flutter_local_notifications: ^17.2.1 
  win_toast: ^0.3.1
  tray_manager: ^0.5.0
  window_manager: ^0.3.7
  flutter_secure_storage: ^9.0.0
  flutter_dotenv: ^5.1.0
  crypto: ^3.0.3
  encrypt: ^5.0.3
  gestures: ^1.0.0
  # Supabase for backend REST API
  supabase_flutter: ^2.17.2

  fl_chart: ^0.63.0

  # Flutter Gemma dependencies
  flutter_gemma: ^1.7.3

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8
  url_launcher: ^6.3.1
  app_links: ^7.2.1

  # File export/import dependencies
  file_picker: ^5.0.0
  share_plus: ^12.0.2
  path_provider: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Drift code generation dependencies
  drift_dev: ^2.16.0
  build_runner: ^2.4.0
  source_gen: ^4.1.0

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true

  # To add assets to your application, add an assets section, like this:
  assets:
      - assets/sqlite3.dll

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package