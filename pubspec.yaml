name: maximize
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  table_calendar: ^3.0.0
  drift: ^2.0.0  # Check for the latest version
  path_provider: ^2.0.15
  path: ^1.8.3
  json_annotation: ^4.9.0  # Check for the latest version
  drift_flutter: 0.2.3  # For Flutter integration
  sqlite3: ^2.5.0  # Required for SQLite support
  json_serializable: ^6.9.0
  provider: ^6.0.1
  shared_preferences: ^2.0.0
  uuid: ^3.0.7
  drift_sqflite: ^2.0.1
  sqflite: ^2.0.0
  flutter_colorpicker: ^1.1.0
  timezone: ^0.9.2
  http: ^1.4.0
  flutter_local_notifications: ^17.2.1 
  win_toast: ^0.3.1
  flutter_quill: ^9.3.4
  tray_manager: ^0.5.0
  window_manager: ^0.3.7

  # The following adds the Cupertino Icons font to your application.
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Drift code generation dependencies
  build_runner: ^2.0.14  # Required for code generation
  drift_dev: ^2.0.0  # Required for Drift code generation

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
      - assets/sqlite3.dll

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package